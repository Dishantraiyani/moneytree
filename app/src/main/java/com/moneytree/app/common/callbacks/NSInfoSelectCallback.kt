package com.moneytree.app.common.callbacks

/**
 * The interface to listen the click on Info select
 */
interface NSInfoSelectCallback {

    /**
     * Invoked when the info click
     */
    fun onClick(position: Int)
}