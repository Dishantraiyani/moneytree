package com.moneytree.app.ui.splash

import android.app.Application
import com.moneytree.app.common.NSViewModel

/**
 * The view model class for Splash handling. It handles the business logic to communicate with the model
 * for the Splash and provides the data to the observing UI component.
 */
class NSSplashViewModel(application: Application) : NSViewModel(application)